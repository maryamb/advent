use std::fmt::Error;
// use core::prelude;
use std::fs::File;
use std::io::{self, BufRead};
use std::default::Default;

// use regex::Match;

// use std::iter::zip;
// use crate::io::Error;
// use std::collections::{HashMap, HashSet};

enum Module {
  FlipFlop(),
  Conjunction,
  Broadcast,
}



fn main() {
  // part_one();
  // part_two();
}